from setuptools import setup, find_packages

setup(
    name="rpbuild",
    version="0.1",
    packages=[
        "rpbuild",
    ],
    #install_requires=[
    #    'some_dependency',
    #    'another_dependency',
    #],
    # ... other metadata
)
